# Allow installing packages with unsatisfied peer dependencies.
# Needed because @azure/msal-react@^2 declares peer react "^16.8||^17||^18"
# but this project uses React 19. The library works at runtime; this flag
# avoids install failures in strict-peer-deps environments (CI, fresh clones).
legacy-peer-deps=true
