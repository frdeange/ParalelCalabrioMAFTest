"""FastAPI dependencies.

Today this package only exposes :mod:`app.deps.identity` (caller
authentication via APIM-signed headers); future deps (rate-limiting,
audit context, feature flags) live alongside it.
"""
