"""Security primitives shared across the backend.

The package only contains a single module today (:mod:`app.security.hmac`)
but exists as a package so future additions (JWT helpers, encryption
utilities) have a stable home.
"""
